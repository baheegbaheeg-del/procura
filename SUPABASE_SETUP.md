# تشغيل صِلَة ببيانات حقيقية

هذه الحزمة تحتوي على قاعدة البيانات وسياسات الوصول والمرفقات الخاصة بصِلَة.

1. افتح مشروع Supabase جديدًا من لوحة التحكم، واختر منطقة قريبة من المستخدمين، ثم انتظر حتى يصبح المشروع جاهزًا.
2. افتح **SQL Editor** وشغّل ملف `supabase/schema.sql` كاملًا مرة واحدة. ينشئ هذا الجداول والصلاحيات وتخزين المرفقات وأرقام المستندات المتسلسلة.
3. من **Authentication** فعّل تسجيل الدخول بالبريد وكلمة المرور، وأضف رابط الموقع النهائي ضمن **Redirect URLs**. إذا فعّلت تأكيد البريد، سيحتاج المستخدم إلى تأكيده قبل أول دخول.
3. سجّل أول حساب من واجهة صِلَة، ثم حوّله إلى إدارة مرة واحدة من SQL Editor:

   ```sql
   update public.profiles
   set role = 'admin', verification_status = 'موثق'
   where email = 'admin@your-company.com';
   ```

4. انسخ رابط المشروع من **Integrations → Data API**، ثم انسخ **Publishable key** من **Settings → API Keys** إلى `supabase-config.js`:

   ```js
   window.SILAH_SUPABASE = {
     url: 'https://your-project.supabase.co',
     publishableKey: 'sb_publishable_...'
   };
   ```

5. ارفع الملفات إلى Vercel. لا تضع كلمة مرور قاعدة البيانات أو `service_role` أو Secret key في أي ملف متاح للمتصفح.

## ما يحميه الإعداد

- حساب منفصل لكل عميل أو مورد أو إدارة.
- سياسات Row Level Security: العميل لا يرى إلا طلباته وأوامره، والمورد يرى فرص التسعير وأوامره، والإدارة تراجع الجميع.
- مرفقات خاصة، مع روابط مؤقتة للفتح والتنزيل.
- مستندات السجل التجاري والضريبي والتفويض لا تظهر إلا للحسابات المسجلة، وبعد اعتماد الحساب كـ«موثق».
- انتقال حالة أمر الشراء مضبوط من قاعدة البيانات: المورد يجهّز ويورد، والعميل وحده يؤكد الاستلام النهائي.

## تنبيه قبل الإطلاق

اختبر السياسات بحساب عميل وحساب مورد منفصلين قبل فتح التسجيل للناس. عدّل حد حجم الملفات وسياسة الاحتفاظ بالمرفقات وفق احتياج النشاط والقوانين المحلية.
